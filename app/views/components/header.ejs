<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a id="header-left-link" class="navbar-brand" href="/"><i class="fa-solid fa-link fa-lg"></i><img id="header-logo" src="/content/images/survey13-logo.jpg">Survey13</a>

        <!-- *** also insert main website logo here -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- navigation to different tabs -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a id="header-home-effect" class="nav-link <%= (page == 'home') ? 'active' : ''; %>" aria-current="page" href="/"><i
                class="fa-solid fa-house-signal fa-lg"></i> Home</a>
                </li>

                <% if(displayName) { %>
                    <li class="nav-item">
                      <a class="nav-link <%= (page == 'logout') ? 'active' : ''; %>" aria-current="page" href="/logout"><i class="fa-solid fa-right-from-bracket fa-lg"></i> Logout</a>
                    </li> 
                    <% } else { %>
                    <li class="nav-item">
                      <a class="nav-link <%= (page == 'login') ? 'active' : ''; %>" aria-current="page" href="/login"><i class="fa-solid fa-right-to-bracket fa-lg"></i> Login</a>
                    </li> 
                    <% } %>
        
            </ul>
        </div>
    </div>
</nav>